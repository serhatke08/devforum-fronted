import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'usupport - DevForum',
  alternates: { canonical: 'https://devforum.xyz/support' }
};

export default function Page() {
  return <div>support page placeholder</div>;
}
